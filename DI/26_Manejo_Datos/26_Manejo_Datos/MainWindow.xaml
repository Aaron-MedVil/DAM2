<Window x:Class="_26_Manejo_Datos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="CRUD JSON" MinHeight="450" MinWidth="800" WindowState="Maximized" Loaded="Window_Loaded">

    <Grid Background="#333" ShowGridLines="True">

        <!-- Definicion de columnas -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.15*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Menu lateral -->
        <DockPanel Style="{StaticResource menu-lateral}">
            <Grid.Column>0</Grid.Column>
            <StackPanel>

                <!-- Boton home -->
                <Button>
                    <md:PackIcon Kind="Home"/>
                </Button>
            </StackPanel>
        </DockPanel>

        <!-- Contenido -->
        <DockPanel x:Name="contPages" Grid.Column="1"/>
    </Grid>
</Window>